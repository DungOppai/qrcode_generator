# QR Code Generator & Scanner (React + Vite)

Ứng dụng tạo QR code từ text/link và đọc QR code từ camera, triển khai bằng React (Vite). Hỗ trợ offline (Service Worker) và lưu dữ liệu cơ bản bằng localStorage.

## Cài đặt & chạy
```bash
npm install
npm run dev
```
